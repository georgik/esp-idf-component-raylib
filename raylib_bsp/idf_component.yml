description: Raylib BSP Abstraction Layer
license: Zlib
url: https://github.com/georgik/esp-idf-component-raylib
version: 1.0.0

dependencies:
  idf:
    version: ">=5.1.0"
  # Note: BSP dependencies must be declared in the project's manifest
  # when using override_path for local development
